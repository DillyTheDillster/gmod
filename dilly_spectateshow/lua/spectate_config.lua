DSPECTATE.switchDelay = 0.4;// The delay between switching spectate between users.
DSPECTATE.camBack = 100; // How far back should the spectate camera be?
DSPECTATE.spectateText = "Spectators: "; // The text above spectating users.
DSPECTATE.comamnd = "!ob"; // The chat command to observe a user.
DSPECTATE.nameTextColor = Color(255, 255, 255); // The color of speactating users names.
DSPECTATE.tagColor = Color(200, 100, 50); // Color of the tag in the messages.
DSPECTATE.messageTextColor = Color(255, 255, 255); // Color of the messages text.